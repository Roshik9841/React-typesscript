export const User = ()=>{
    const handleLogin = ()=>{

    }
    const handleLogOut = ()=>{

    }

    return(
        <div>
            <button onClick={handleLogin}>
                Login
            </button>
            <button onClick = {handleLogOut}>
                Logout
            </button>
            <div>
                User name is 
            </div>
        </div>
    )
}